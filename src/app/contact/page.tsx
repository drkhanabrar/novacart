import Link from "next/link";
import { Mail, Package, ShieldCheck } from "lucide-react";

export default function ContactPage() {
  const supportEmail = process.env.NEXT_PUBLIC_SUPPORT_EMAIL;
  return <main className="mx-auto max-w-4xl px-5 py-12 sm:px-6 sm:py-16"><Link href="/" className="text-sm font-semibold text-ink-soft hover:text-poppy">← Back to NovaCart</Link><div className="mt-8"><span className="section-kicker">Contact</span><h1 className="mt-2 font-display text-4xl italic text-ink sm:text-5xl">How can we help?</h1><p className="mt-4 max-w-2xl text-sm leading-7 text-ink-soft">For order questions, delivery issues, returns or account help, keep your order number handy so we can help you faster.</p><div className="mt-10 grid gap-5 md:grid-cols-3"><div className="rounded-[2rem] border border-ink/10 bg-card p-6 shadow-sm"><Package className="h-5 w-5 text-poppy"/><h2 className="mt-4 text-lg font-bold text-ink">Orders</h2><p className="mt-2 text-sm leading-6 text-ink-soft">Use My Orders to see the latest status and order details.</p><Link href="/orders" className="mt-5 inline-flex text-sm font-semibold text-poppy">Open orders →</Link></div><div className="rounded-[2rem] border border-ink/10 bg-card p-6 shadow-sm"><Mail className="h-5 w-5 text-sage"/><h2 className="mt-4 text-lg font-bold text-ink">Email support</h2>{supportEmail ? <a href={`mailto:${supportEmail}`} className="mt-2 block text-sm font-semibold text-poppy hover:underline">{supportEmail}</a> : <p className="mt-2 text-sm leading-6 text-ink-soft">Support email will appear here once it is configured for the live store.</p>}</div><div className="rounded-[2rem] border border-ink/10 bg-card p-6 shadow-sm"><ShieldCheck className="h-5 w-5 text-marigold"/><h2 className="mt-4 text-lg font-bold text-ink">Account help</h2><p className="mt-2 text-sm leading-6 text-ink-soft">Your saved address, phone and order information live inside your secure account.</p><Link href="/account" className="mt-5 inline-flex text-sm font-semibold text-poppy">Open account →</Link></div></div></div></main>;
}
