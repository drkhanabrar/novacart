"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link"; import { loginUser } from "@/actions/auth"; import { ArrowRight, Lock, Mail } from "lucide-react";
export default function LoginPage(){const router=useRouter();const[error,setError]=useState<string|null>(null);const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError(null);const r=await loginUser(new FormData(e.currentTarget));if(r.error){setError(r.error);setLoading(false);return}const next=new URLSearchParams(window.location.search).get("next");router.push(next&&next.startsWith("/")?next:"/products");router.refresh()}return <div className="min-h-[80vh] flex items-center justify-center px-4 py-14"><div className="w-full max-w-md rounded-[30px] border border-ink/10 bg-card p-8 shadow-xl shadow-ink/5 sm:p-10"><div className="text-center mb-8"><img src="/logo.png" alt="NovaCart" className="mx-auto h-8 w-auto object-contain mb-5"/><span className="font-tag text-[10px] uppercase tracking-[.18em] text-poppy">Welcome back</span><h1 className="font-display italic text-4xl text-ink mt-2">Sign in.</h1><p className="text-sm text-ink-soft mt-1">Access your account, orders and saved addresses.</p></div>{error&&<div className="mb-6 rounded-xl border border-poppy/20 bg-poppy/10 p-4 text-sm text-poppy-dark">{error}</div>}<form onSubmit={submit} className="space-y-5"><label className="block"><span className="font-tag text-[10px] uppercase tracking-wider text-ink-soft">Email address</span><span className="relative block mt-1.5"><Mail className="absolute left-3.5 top-3.5 h-4 w-4 text-ink-soft"/><input name="email" type="email" required className="w-full rounded-xl border border-ink/15 bg-cream px-10 py-3.5 text-sm outline-none focus:border-poppy" placeholder="name@example.com"/></span></label><label className="block"><span className="font-tag text-[10px] uppercase tracking-wider text-ink-soft">Password</span><span className="relative block mt-1.5"><Lock className="absolute left-3.5 top-3.5 h-4 w-4 text-ink-soft"/><input name="password" type="password" required className="w-full rounded-xl border border-ink/15 bg-cream px-10 py-3.5 text-sm outline-none focus:border-poppy" placeholder="••••••••"/></span></label><div className="flex justify-end"><Link href="/forgot-password" className="text-xs font-semibold text-poppy hover:underline">Forgot password?</Link></div><button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-full bg-poppy py-3.5 text-sm font-semibold text-white shadow-lg shadow-poppy/20 hover:bg-poppy-dark disabled:opacity-50">{loading?"Signing in…":"Sign in"}<ArrowRight className="h-4 w-4"/></button></form><p className="mt-7 text-center text-sm text-ink-soft">New to NovaCart? <Link href="/register" className="font-semibold text-poppy hover:underline">Create an account</Link></p></div></div>}
