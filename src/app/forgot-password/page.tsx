"use client";
import { useState } from "react"; import Link from "next/link"; import { requestPasswordReset } from "@/actions/auth"; import { ArrowRight, Mail } from "lucide-react";
export default function ForgotPasswordPage(){const[error,setError]=useState<string|null>(null);const[message,setMessage]=useState<string|null>(null);const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError(null);setMessage(null);const r=await requestPasswordReset(new FormData(e.currentTarget));if("success" in r)setMessage(r.message);else setError(r.error);setLoading(false)}return <div className="min-h-[80vh] flex items-center justify-center px-4 py-14"><div className="w-full max-w-md rounded-[28px] border border-ink/10 bg-card p-8 shadow-xl shadow-ink/5 sm:p-10"><div className="text-center mb-8"><img src="/logo.png" alt="NovaCart" className="mx-auto h-8 w-auto object-contain mb-5"/><span className="font-tag text-[10px] uppercase tracking-[.18em] text-poppy">Account recovery</span><h1 className="font-display italic mt-2 text-4xl text-ink">Reset your password.</h1><p className="mt-2 text-sm text-ink-soft">We&apos;ll send a secure reset link to your email.</p></div>{error&&<div className="mb-6 rounded-xl border border-poppy/20 bg-poppy/10 p-4 text-sm text-poppy-dark">{error}</div>}{message&&<div className="mb-6 rounded-xl border border-sage/25 bg-sage/10 p-4 text-sm text-sage">{message}</div>}<form onSubmit={submit} className="space-y-5"><label className="block"><span className="font-tag text-[10px] uppercase tracking-wider text-ink-soft">Email address</span><span className="relative mt-1.5 block"><Mail className="absolute left-3.5 top-3.5 h-4 w-4 text-ink-soft"/><input name="email" type="email" required className="w-full rounded-xl border border-ink/15 bg-cream px-10 py-3.5 text-sm outline-none focus:border-poppy" placeholder="name@example.com"/></span></label><button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-full bg-poppy py-3.5 text-sm font-semibold text-white hover:bg-poppy-dark disabled:opacity-50">{loading?"Sending…":"Send reset link"}<ArrowRight className="h-4 w-4"/></button></form><p className="mt-6 text-center text-sm text-ink-soft">Remembered your password? <Link href="/login" className="font-semibold text-poppy hover:underline">Back to sign in</Link></p></div></div>}
